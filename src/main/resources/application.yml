#spring.application.name=tldusalstjgram
#
#spring.thymeleaf.cache=false

spring:
  jpa:
    hibernate:
      ddl-auto: none      
  mvc:
    static-path-pattern: /static/**     
  config:
    import: security.yml      
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kbo
    username: root
    password: ${mysql.password}               
  thymeleaf:
    cache: false
  application:
    name: kbc
mybatis:
  mapper-locations: classpath:/mappers/*Mapper.xml                                    